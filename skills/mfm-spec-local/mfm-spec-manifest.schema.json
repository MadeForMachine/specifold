{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://madeformachine.com/mfm-spec/0.3/mfm-spec-manifest.schema.json",
  "title": "MFM Spec root manifest (v0.3)",
  "description": "The mfm-spec.yaml root manifest. Makes a spec self-identifying: format version and entry point before any node is parsed.",
  "type": "object",
  "additionalProperties": false,
  "required": ["spec_format", "name", "root"],
  "properties": {
    "spec_format": { "const": "0.3" },
    "name": { "type": "string", "minLength": 1 },
    "root": { "type": "string", "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$" },
    "created": { "type": "string", "format": "date" },
    "invariants": { "type": "array", "items": { "type": "string" } }
  }
}
